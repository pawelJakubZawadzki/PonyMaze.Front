export const selectMazeId = state => state.maze.mazeId;

export const selectMazeData = state => state.maze.mazeData;

export const selectMazeSize = state => state.maze.mazeSize;

export const selectPonyLocation = state => state.maze.ponyLocation;

export const selectDomokunLocation = state => state.maze.domokunLocation;

export const selectEndpointLocation = state => state.maze.endpointLocation;
